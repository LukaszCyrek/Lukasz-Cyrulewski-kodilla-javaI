package array;

import org.junit.Test;

import java.util.stream.IntStream;

public class ArrayOperationsTestSuite {
    @Test
    public void testGetAverage() {
        Integer [] numbers = {2, 3, 1, 4, 2, 5, 5};

      //  Double average = IntStream.range(0, numbers.length());
      //  .average();
    }
}
